import React from 'react';
import './header.css';

const Header = () => (
	<header className="header">
		<div className="logo">
			<img src="img/logo.png" className="logo__img" alt="logo" />
		</div>
	</header>
);

export default Header;